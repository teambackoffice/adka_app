import 'package:flutter/material.dart';

class FieldStaffBreakdownPage extends StatelessWidget {
  const FieldStaffBreakdownPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Breakdown')));
  }
}
